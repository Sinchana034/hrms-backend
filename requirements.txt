# Web framework
fastapi==0.115.0
uvicorn==0.30.6
python-multipart==0.0.9

# Config / validation
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.2.2
email-validator==2.3.0

# Supabase client + its own sub-dependencies
supabase==2.7.4
postgrest==0.16.11
gotrue==2.12.4
storage3==0.7.7
realtime==2.5.3
supafunc==0.5.1

# Auth (JWT verification against Supabase JWKS)
python-jose==3.3.0
cryptography==50.0.0

# HTTP client (JWKS fetch, CAPTCHA verify, Gmail API calls)
httpx==0.27.2

# Rate limiting
slowapi==0.1.9

# Duplicate detection (fuzzy name matching)
RapidFuzz==3.14.5

# Resume parsing
pypdf==6.16.2
python-docx==1.2.0

# ML: resume matching / shortlisting model (ml/train_model.py, ml_predictor.py)
scikit-learn==1.7.1
pandas==2.3.1
numpy==2.3.2
joblib==1.5.1

# Offer letter PDF generation
reportlab==5.0.1

# Gmail sync (Phase 2 email ingestion)
google-auth==2.57.0
google-auth-oauthlib==1.4.1
google-api-python-client==2.187.0

email-validator
gunicorn